CREATE TABLE Students(
    id int PRIMARY KEY,
    first_name varchar(255),
    last_name varchar(255),
    reg_id varchar(255),
    branch varchar(255),
    cgpa double precision,
    backlog_count int,
);